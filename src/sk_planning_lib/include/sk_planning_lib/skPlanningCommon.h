#ifndef _SK_PLANNING_COMMON_H_
#define _SK_PLANNING_COMMON_H_

#include <cmath>
#include <limits>

using namespace std;

#define MAX(x, y) (((x) > (y)) ? (x) : (y))
#define MIN(x, y) (((x) < (y)) ? (x) : (y))

#define SK_CELL_FEASIBLE (0)
#define SK_CELL_INFEASIBLE (1)
#define SK_CELL_UNKNOWN (-1)


#endif // _SK_PLANNING_COMMON_H_
